[project]
name = "medliner"
version = "0.1.0"
description = "Reproducible GLiNER fine-tuning and evaluation for medical indication, contraindication, and drug NER."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  # accelerate backs the HF Trainer loop that gliner.training.Trainer extends.
  "accelerate>=1.1,<2",
  "gliner==0.2.28",
  # Imported directly (TrainerCallback, TrainingArguments); not left to gliner's transitive pin.
  "transformers>=4.46",
  "torch>=2.9,<3",
  "pydantic>=2.8,<3",
  "pyyaml>=6,<7",
  # Not imported by MEDliNER itself. `make_dakp_gazetteer_predictor` imports the sibling DAKP
  # gazetteer into this process for the baseline comparison that gates checkpoint selection,
  # and that import pulls in DAKP's own runtime dependencies. Keep them in sync with
  # `dakp_pipeline.ner.ner`'s import chain; the `evaluation_report` asset reports the
  # ImportError in `baselines.dakp_gazetteer_error` rather than failing when one is missing.
  "blake3>=1,<2",
  "loguru>=0.7,<1",
  "polars>=1.30,<2",
]

[dependency-groups]
dev = [
  "pytest>=8,<9",
  "pytest-cov>=5",
  "ruff>=0.8,<1",
]

[project.scripts]
medliner = "medliner.cli:main"

[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[tool.uv.sources]
# The laptop RTX 5070 Ti is compute capability sm_120. Use a CUDA wheel that contains
# sm_120 kernels; the cu126 wheel used by the sibling DAKP/P100 environment does not.
torch = { index = "pytorch-cu130" }

[[tool.uv.index]]
name = "pytorch-cu130"
url = "https://download.pytorch.org/whl/cu130"
explicit = true

[tool.hatch.build.targets.wheel]
packages = ["src/medliner"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 120
target-version = "py312"
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I", "UP", "B"]
